const fmt=(n,d=2)=>Number(n).toLocaleString('pt-BR',{minimumFractionDigits:d,maximumFractionDigits:d});
let model=null;
async function load(){
 const r=await fetch('data/latest.json?ts='+Date.now()); model=await r.json();
 const manual=localStorage.getItem('mphi_manual'); if(manual){const m=JSON.parse(manual);if(m.date>=model.current.date){model.meta.source_status='manual';model.meta.updated_at=m.date+'T08:00:00-04:00';model.current.date=m.date;model.current.level=Number(m.level);model.meta.source_note='Valor manual local aplicado neste navegador. Recalcular o MPHI no fluxo oficial antes de publicar.'}}
 render();
}
function render(){const c=model.current;
 document.getElementById('level').textContent=fmt(c.level);document.getElementById('delta24').textContent=(c.delta_cm>0?'+':'')+fmt(c.delta_cm,0)+' cm';document.getElementById('phase').textContent=c.phase;document.getElementById('peak').textContent=fmt(c.peak)+' m';document.getElementById('peakDate').textContent=new Date(c.peak_date+'T12:00:00').toLocaleDateString('pt-BR');document.getElementById('drawdown').textContent=fmt(c.drawdown)+' m';document.getElementById('persistDays').textContent=c.persistence_days;
 document.getElementById('updatedAt').textContent='Atualizado em '+new Date(model.meta.updated_at).toLocaleString('pt-BR',{timeZone:'America/Manaus',day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});
 const badge=document.getElementById('statusBadge');badge.textContent=c.status;badge.className='status '+(c.status==='NORMAL'?'normal':c.status==='ALTO'?'high':'attention');document.getElementById('score').textContent=c.score;document.getElementById('scoreStatus').textContent=c.status;document.getElementById('sourceNote').textContent=model.meta.source_note;
 const rates=[['3 dias',c.avg3],['7 dias',c.avg7],['15 dias',c.avg15],['30 dias',c.avg30]];document.getElementById('ratesGrid').innerHTML=rates.map(([k,v])=>`<div class="rate ${v<0?'negative':''}"><span>VELOCIDADE · ${k}</span><b>${v>0?'+':''}${fmt(v,2)} cm/d</b></div>`).join('');
 const m=Number(c.date.slice(5,7));const sea=model.seasonal[String(m).padStart(2,'0')]||{};const seasonalPts=sea.q1&&c.level<sea.q1?2:sea.median&&c.level<sea.median?1:0;const velPts=c.avg7<=-12?2:c.avg7<=-8?1:0;const accPts=c.accel_7_15<=-2?2:c.accel_7_15<=-.5?1:0;const pk=model.june_peak_reference;const peakPts=c.peak<pk.q1?2:c.peak<pk.median?1:0;const ddPts=c.drawdown>=4?2:c.drawdown>=3?1:0;const persPts=c.persistence_days>=7?2:0;
 const comps=[['Posição sazonal',seasonalPts],['Velocidade 7d',velPts],['Aceleração 7×15',accPts],['Pico antecedente',peakPts],['Drawdown',ddPts],['Persistência',persPts]];document.getElementById('scoreComponents').innerHTML=comps.map(([n,p])=>`<div class="component"><span>${n}</span><b>${p}/2</b></div>`).join('');
 document.getElementById('projectionRows').innerHTML=Object.entries(model.projections).map(([d,p])=>`<div class="proj"><b>${d} dias</b><span>suave ${fmt(p.soft)}m</span><span class="central">central ${fmt(p.central)}m</span><span>estresse ${fmt(p.stress)}m</span><span>${p.confidence}</span></div>`).join('');
 document.getElementById('seasonCards').innerHTML=Object.entries(model.seasonal).map(([m,s])=>`<div class="season-card"><b>${m==='08'?'Agosto':m==='09'?'Setembro':m}</b><p>Q1 · ${fmt(s.q1)} m<br>Mediana · ${fmt(s.median)} m</p></div>`).join('');drawChart();
}
function drawChart(){const canvas=document.getElementById('riverChart'),ctx=canvas.getContext('2d'),dpr=devicePixelRatio||1,rect=canvas.getBoundingClientRect();canvas.width=rect.width*dpr;canvas.height=rect.height*dpr;ctx.scale(dpr,dpr);const W=rect.width,H=rect.height,pad={l:48,r:18,t:18,b:32};const pts=model.series.filter(x=>x.level!=null),vals=pts.map(x=>x.level),min=Math.floor((Math.min(...vals)-.7)*2)/2,max=Math.ceil((Math.max(...vals)+.6)*2)/2;const x=i=>pad.l+i*(W-pad.l-pad.r)/(pts.length-1),y=v=>pad.t+(max-v)*(H-pad.t-pad.b)/(max-min);ctx.clearRect(0,0,W,H);ctx.font='10px system-ui';ctx.fillStyle='#78918f';ctx.strokeStyle='rgba(154,176,174,.10)';ctx.lineWidth=1;for(let v=Math.ceil(min);v<=max;v++){ctx.beginPath();ctx.moveTo(pad.l,y(v));ctx.lineTo(W-pad.r,y(v));ctx.stroke();ctx.fillText(v.toFixed(0)+'m',7,y(v)+3)}const sea=model.seasonal[model.current.date.slice(5,7)];if(sea){[['q1','#E6A23C'],['median','#8197a0']].forEach(([k,col])=>{ctx.setLineDash([5,5]);ctx.strokeStyle=col;ctx.globalAlpha=.5;ctx.beginPath();ctx.moveTo(pad.l,y(sea[k]));ctx.lineTo(W-pad.r,y(sea[k]));ctx.stroke();ctx.globalAlpha=1;ctx.setLineDash([])})}ctx.strokeStyle='#36C2B4';ctx.lineWidth=2.2;ctx.beginPath();pts.forEach((p,i)=>i?ctx.lineTo(x(i),y(p.level)):ctx.moveTo(x(i),y(p.level)));ctx.stroke();const last=pts.length-1;ctx.fillStyle='#36C2B4';ctx.beginPath();ctx.arc(x(last),y(pts[last].level),4,0,Math.PI*2);ctx.fill();ctx.fillStyle='#F3F6F4';ctx.font='11px system-ui';ctx.fillText(fmt(pts[last].level)+'m',Math.max(pad.l,x(last)-55),y(pts[last].level)-10);for(let i=0;i<pts.length;i+=Math.max(1,Math.floor(pts.length/5))){ctx.fillStyle='#78918f';ctx.font='9px system-ui';ctx.fillText(new Date(pts[i].date+'T12:00:00').toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit'}),x(i)-12,H-9)}}
window.addEventListener('resize',()=>model&&drawChart());const dlg=document.getElementById('maintenanceDialog');document.getElementById('maintenanceBtn').onclick=()=>{document.getElementById('manualDate').value=model.current.date;document.getElementById('manualLevel').value=model.current.level;dlg.showModal()};document.getElementById('saveManual').onclick=()=>{const date=document.getElementById('manualDate').value,level=document.getElementById('manualLevel').value;if(!date||!level)return;localStorage.setItem('mphi_manual',JSON.stringify({date,level}));dlg.close();load()};load();